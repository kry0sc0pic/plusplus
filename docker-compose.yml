services:
  redis:
    image: redis:alpine
    volumes:
      - redis-data:/data
    restart: unless-stopped
  bot:
    build: .
    env_file:
      - .env
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis
    restart: unless-stopped

volumes:
  redis-data:
